import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tptmbds.egouvernance',
  appName: 'e-gouvernance',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
